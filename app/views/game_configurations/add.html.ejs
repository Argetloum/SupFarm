<div class="hero-unit">
  <form id="gameConfiguration-form" class="form-horizontal" action="/game_configurations" method="POST">
    <fieldset>
      <legend>Create a new GameConfiguration</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {gameConfiguration: {}}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
